import { Request, Response, NextFunction } from "express";

export class Controller{

static createFolderController(req: Request, res: Response, next: NextFunction) {
 
    try {
        
    }catch(error){
        next(error)
    }
    
}
    


}